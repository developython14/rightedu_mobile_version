import 'package:flutter/material.dart';
import 'package:status_change/status_change.dart';

class reed extends StatefulWidget {
  const reed({Key? key}) : super(key: key);

  @override
  State<reed> createState() => _reedState();
}

class _reedState extends State<reed> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('packag')),
      body: Center(
        child: Text('packag'),
      ),
    );
  }
}
